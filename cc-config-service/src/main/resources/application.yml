server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cher-yami/NEWWW.git
          searchPaths: config-data
          default-label: main
  security:
    user:
      name: Cher
      password: 202211951
